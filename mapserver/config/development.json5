{
  dirs: {
    tiles: "/home/martin/fm/maprender/mapserver/tiles",
    // expires: "expires",
    hillshading: "/home/martin/14TB/hillshading",
    svg: "/home/martin/fm/maprender/rust/images",
  },
  server: {
    port: 4000,
  },
  forceTileRendering: true, // useful for style development
  mapFeatures: {
    contours: false,
    shading: true,
    hikingTrails: true,
    bicycleTrails: true,
    skiTrails: true,
    horseTrails: true,
  },
  limits: {
    minZoom: 0,
    maxZoom: 20,
    polygon: "/home/martin/fm/maprender/mapserver/limit-shading.geojson",
    scales: [1, 2, 3],
    cleanup: true,
  },
  // set to null to disable pre-rendering
  prerender: null,
  rerenderOlderThanMs: 0, // use 0 to disable
  exportMapConcurrency: 1,
  format: {
    extension: "jpg",
    mimeType: "image/jpeg",
  },
  minExpiredBatchSize: 500,
  expiresZoom: 14, // imposm3 is configured to check for expired tiles on zoom 14
  prerenderMaxZoom: 14, // we need to know it even in on-demand only instance for creating index files
  // how long to delay rendering a tile when expiring tiles
  // this is to prevent drastical fi i/o slowdown when prerendering eats 100% cpu
  // set to zero to prevent this feature
  prerenderDelayWhenExpiring: 200,
  postgresConnectionString: "postgres://martin:b0n0@localhost/martin",
}
